#pragma once

#include <string>

namespace trip
{
    struct TripInfo
    {
        std::string title;
        std::string start_date;
        std::string end_date;
        std::string description;
    };

}
